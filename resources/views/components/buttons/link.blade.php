<a  href="{{ route( $btnLink ) }}"
    class="{{ $btnClass }} {{$btnColor}}"
    role="button">

    <i class="{{$btnIcon}} align-baseline"></i>

    <span class="d-none d-md-inline align-text-baseline">
      {{ $btnName }}
    </span>

</a>
